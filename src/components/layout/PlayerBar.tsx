// Thin wrapper so the layout tree keeps importing `PlayerBar` from
// `@/components/layout/PlayerBar`. The implementation now lives in
// `src/playback/components/PlayerBar.tsx` next to its sibling
// components.

export { PlayerBar } from "@/playback/components/PlayerBar";
